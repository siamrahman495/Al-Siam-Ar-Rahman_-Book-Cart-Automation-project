from playwright.sync_api import Playwright, sync_playwright, expect

def run(playwright: Playwright) -> None:
    browser = playwright.chromium.launch(headless=False)
    context = browser.new_context()
    page = context.new_page()
    page.goto("https://bookcart.azurewebsites.net/")
    
    page.get_by_role("button", name="Login").click()
    page.get_by_placeholder("Username").fill("fake@gmail.com")
    page.get_by_placeholder("Password").fill("faklertyf")
    page.get_by_role("button", name="Login").nth(1).click()

    # Optional: You can use expect to check for an error message
    # Example: expect(page.get_by_text("Invalid username or password")).to_be_visible()

    context.close()
    browser.close()

with sync_playwright() as playwright:
    run(playwright)
